# ── Postgres ──
# Bitta URL: ish vaqti ham, migratsiya ham shundan foydalanadi.
DATABASE_URL="postgresql://FOYDALANUVCHI:PAROL@127.0.0.1:5432/starspaymee"

# ── Redis ──
REDIS_URL="redis://127.0.0.1:6379"
# Ikki brend bitta Redis'ni baham ko'rsa — har brendga o'z prefiksi
REDIS_KEY_PREFIX="sp"

# ── Auth ──
# openssl rand -base64 48
JWT_SECRET=""

# ── Cloudflare (narx o'zgarganda edge cache purge) ──
CF_ZONE_ID=""
CF_API_TOKEN=""

# ── Kirish darvozasi: Cloudflare Turnstile (tekin) ──
# dash.cloudflare.com → Turnstile → Add widget
#   Mode: Managed, Domains: milliyjamoasimiz.uz (+ localhost, dev uchun)
# Bo'sh qoldirilsa yoki GATE_ENABLED=0 bo'lsa — darvoza o'chadi, sayt oddiy ochiladi.
GATE_ENABLED="1"
NEXT_PUBLIC_TURNSTILE_SITE_KEY=""
TURNSTILE_SECRET_KEY=""

# ── Sayt ──
SITE_URL="https://milliyjamosimiz.uz"
NEXT_PUBLIC_BOT="Openbyudgeet_bot"

# ── Meta Pixel (ixtiyoriy) ──
# SAYT BO'YICHA UMUMIY pixel. Har sahifaning O'Z pixeli esa admin panelidan
# beriladi (Sozlamalar → Meta Pixel) va shu ro'yxatga QO'SHILADI.
# Events Manager'dagi 15-16 xonali ID. Bir nechtasi vergul bilan:
#   "111111111111111,222222222222222,333333333333333"
# Har pixel 8 ta landing sahifasida PageView, har CTA bosilishida Lead oladi.
# DIQQAT: NEXT_PUBLIC_* build'ga muhrlanadi — o'zgartirsangiz qayta build shart.
NEXT_PUBLIC_META_PIXEL_ID=""

# ── Faqat seed uchun (keyin o'chirib tashlang) ──
ADMIN_USER="admin"
ADMIN_PASS=""
